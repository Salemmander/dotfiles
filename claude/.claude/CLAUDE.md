# User Preferences

- Never include the Claude Code reference or Co-Authored-By line in commit messages
- Never use emojis in any files
- Avoid using `cd` to change directories in bash commands. Use paths relative to the starting working directory instead.
