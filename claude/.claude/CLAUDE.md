@~/.claude/local.md

- Work autonomously. Only confirm first when a change is large, ambiguous, or hard to undo
- Be concise
- For ad-hoc Python outside a project, use `uv run --no-project --with <pkg> python ...` (`--no-project` keeps uv from creating `uv.lock`/`.venv` in the current dir)
