# Extra autostart processes
# exec-once = uwsm app -- my-service

# Apply clamshell state on startup (boot with lid already closed).
# The lid-close.sh script checks the actual lid state, so it's a no-op when open.
exec-once = ~/.config/hypr/scripts/lid-close.sh
